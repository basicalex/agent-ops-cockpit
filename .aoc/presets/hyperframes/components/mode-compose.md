# Compose mode

Create and iterate HyperFrames videos.

Use the `hyperframes` skill for composition rules and the `gsap` skill for timeline animation. Build static layout first, then add GSAP entrances/exits. Keep media, audio, captions, timing, and aspect ratio explicit.
