#!/usr/bin/env bash
set -euo pipefail

agent_id="oc"
agent_bin="${AOC_OC_BIN:-opencode}"
agent_label="OpenCode"
export AOC_OC_VERSION="${AOC_OC_VERSION:-1.1.25}"

exec aoc-agent-wrap "$agent_id" "$agent_bin" "$agent_label" "$@"
